#ifndef TYPES_H
#define TYPES_H

// Forwarding header for backward compatibility
#include "../Models/Types.h"

// Pull Models into global namespace (for now, to avoid breaking existing code)
using namespace FactoryAgent::Models;

#endif